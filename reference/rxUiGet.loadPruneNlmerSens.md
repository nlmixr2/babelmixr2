# Load pruned nlmer model into symengine (with sensitivity promotion)

Load pruned nlmer model into symengine (with sensitivity promotion)

## Usage

``` r
# S3 method for class 'loadPruneNlmerSens'
rxUiGet(x, ...)
```

## Arguments

- x:

  rxUiGet list(ui)

- ...:

  additional arguments (currently ignored)
