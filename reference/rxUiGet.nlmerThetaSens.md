# Compute d(state)/d(`THETA[i]`) sensitivities for nlmer

Compute d(state)/d(`THETA[i]`) sensitivities for nlmer

## Usage

``` r
# S3 method for class 'nlmerThetaS'
rxUiGet(x, ...)
```

## Arguments

- x:

  rxUiGet list(ui)

- ...:

  additional arguments (currently ignored)
