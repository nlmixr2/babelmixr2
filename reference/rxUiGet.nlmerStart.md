# Starting values for nlmer fixed effects

Starting values for nlmer fixed effects

## Usage

``` r
# S3 method for class 'nlmerStart'
rxUiGet(x, ...)
```

## Arguments

- x:

  rxUiGet list(ui)

- ...:

  additional arguments (currently ignored)

## Value

Named numeric vector: iniDf estimates for thetas, 0 for nonMuEtas
