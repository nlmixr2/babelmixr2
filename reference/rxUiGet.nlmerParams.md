# params() string for the nlmer `THETA[i]`-indexed model

params() string for the nlmer `THETA[i]`-indexed model

## Usage

``` r
# S3 method for class 'nlmerParams'
rxUiGet(x, ...)
```

## Arguments

- x:

  rxUiGet list(ui)

- ...:

  additional arguments (currently ignored)
