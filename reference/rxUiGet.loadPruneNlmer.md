# Load pruned nlmer model into symengine (no sensitivity promotion)

Load pruned nlmer model into symengine (no sensitivity promotion)

## Usage

``` r
# S3 method for class 'loadPruneNlmer'
rxUiGet(x, ...)
```

## Arguments

- x:

  rxUiGet list(ui)

- ...:

  additional arguments (currently ignored)
