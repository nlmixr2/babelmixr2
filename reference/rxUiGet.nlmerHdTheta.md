# Compute d(f)/d(`THETA[i]`) for all nlmer parameters

Compute d(f)/d(`THETA[i]`) for all nlmer parameters

## Usage

``` r
# S3 method for class 'nlmerHdTheta'
rxUiGet(x, ...)
```

## Arguments

- x:

  rxUiGet list(ui)

- ...:

  additional arguments (currently ignored)
