% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pharmmlControl.R
\name{pharmmlControl}
\alias{pharmmlControl}
\title{Control options for PharmML export}
\usage{
pharmmlControl(
  version = "0.9",
  validate = TRUE,
  dataFile = NULL,
  description = NULL,
  writeData = TRUE
)
}
\arguments{
\item{version}{PharmML version to write.  See \code{pharmmlVersions()}.}

\item{validate}{When \code{TRUE} (default) the emitted document is validated
against the PharmML schema before it is returned, and an invalid document
is an error.  Validation is offline and takes a fraction of a second, so
there is rarely a reason to turn it off.}

\item{dataFile}{Name the PharmML document should use when referring to the
exported dataset.  When \code{NULL} this is derived from the model name.}

\item{description}{Free text written into the document's \code{ct:Description}.
When \code{NULL} a short provenance line naming \code{babelmixr2} is written.}

\item{writeData}{When \code{TRUE} (default) \code{as.pharmml(file=)} also writes the
NONMEM-format dataset next to the model file.  A PharmML document refers
to its data by path rather than embedding it, so a document written
without its dataset is incomplete.}
}
\value{
a list of PharmML export options
}
\description{
Control options for PharmML export
}
\examples{
pharmmlControl(validate = FALSE)
}
\author{
Matthew L. Fidler
}
