% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pharmmlApi.R
\name{as.pharmml}
\alias{as.pharmml}
\title{Translate an nlmixr2 model to PharmML}
\usage{
as.pharmml(model, data = NULL, file = NULL, control = pharmmlControl())
}
\arguments{
\item{model}{An \code{rxode2}/\code{nlmixr2} model function or ui}

\item{data}{The dataset the model is to be estimated with.  When \code{NULL}
only the \code{ModelDefinition} is written: a valid PharmML model description
with no trial design or estimation step.}

\item{file}{Path to write the document to.  When \code{NULL} (default) nothing
is written and the document is returned.}

\item{control}{\code{pharmmlControl()} options}
}
\value{
the PharmML document, invisibly when \code{file} is given
}
\description{
PharmML is a standard XML description of a pharmacometric model, so this
makes an \code{nlmixr2} model archivable and exchangeable without the receiving
tool needing to understand \code{rxode2} syntax.
}
\details{
The emitted document is validated against the PharmML schema before it is
returned.  Constructs PharmML cannot express -- non-normal residuals,
inter-occasion variability, mixture models -- raise an error naming the
construct rather than producing a silently wrong document.

A PharmML document refers to its dataset by path rather than embedding it,
so when \code{file} is given the NONMEM-format dataset is written alongside the
model unless \code{pharmmlControl(writeData = FALSE)}.
}
\examples{
\dontrun{
one.cmt <- function() {
  ini({
    tka <- log(1.57); tcl <- log(2.72); tv <- log(31.5)
    eta.ka ~ 0.6; eta.cl ~ 0.3; eta.v ~ 0.1
    add.sd <- 0.7
  })
  model({
    ka <- exp(tka + eta.ka)
    cl <- exp(tcl + eta.cl)
    vc <- exp(tv + eta.v)
    d/dt(depot) <- -ka * depot
    d/dt(center) <- ka * depot - cl / vc * center
    cp <- center / vc
    cp ~ add(add.sd)
  })
}
as.pharmml(one.cmt, nlmixr2data::theo_sd, file = "theo.xml")
}
}
\author{
Matthew L. Fidler
}
