% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlmer.R
\name{.nlmerNlmerFun}
\alias{.nlmerNlmerFun}
\title{Surrogate gradient function for lme4::nlmer}
\usage{
.nlmerNlmerFun(TIME, ...)
}
\arguments{
\item{TIME}{Observation times vector (unused; kept for formula
compatibility -- the solver uses the resident per-subject event data)}

\item{...}{Individual-level parameter vectors (one per param in
\code{saemParamsToEstimate})}
}
\value{
Numeric vector of predictions with \code{"gradient"} attribute
}
\description{
Computes predictions and the analytical gradient for every observation
using the nlm C machinery (a single multithreaded \code{\link[nlmixr2est:nlmerSolveGrad]{nlmixr2est::nlmerSolveGrad()}}
call against the model kept resident by \code{\link[nlmixr2est:.nlmSetupEnv]{nlmixr2est::.nlmSetupEnv()}}).
Parameters arrive in positional order matching
\code{.nlmerGlobal$nlmerEnv$paramNames} (THETA\link{i} order).
}
\details{
lme4 passes one value per observation for each nonlinear parameter; within a
subject these are constant (phi = beta + b), so the per-subject parameter
vector is read from each subject's first observation row.
}
