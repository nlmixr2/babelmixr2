% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlmer.R
\name{.nlmerCurrentFormula}
\alias{.nlmerCurrentFormula}
\title{Accessor for the nlmer formula currently being fit}
\usage{
.nlmerCurrentFormula()
}
\value{
The 3-part nlmer formula for the active fit, or \code{NULL} when no fit
is in progress.
}
\description{
\code{lme4::nlmer()} captures \code{match.call()$formula} and re-evaluates it deep in
its own call stack, where the babelmixr2 fit locals are not in scope, so the
formula must be referenced through an exported, namespace-qualified call
rather than a \code{:::} reference to the package's internal \code{.nlmerGlobal}
(which CRAN disallows).  This returns the 3-part formula stashed by
\code{.nlmerFitModel()} for the in-progress fit.
}
\keyword{internal}
