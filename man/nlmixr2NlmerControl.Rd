% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlmerControl.R
\name{nlmixr2NlmerControl}
\alias{nlmixr2NlmerControl}
\alias{nlmerControl}
\title{Control for nlmer estimation method in nlmixr2}
\usage{
nlmixr2NlmerControl(
  optimizer = "bobyqa",
  tolPwrss = NULL,
  optCtrl = list(),
  returnNlmer = FALSE,
  muRefCovAlg = TRUE,
  eventSens = c("jump", "fd"),
  eventType = c("central", "forward"),
  shiErr = (.Machine$double.eps)^(1/3),
  shi21maxFD = 20L,
  stickyRecalcN = 4,
  maxOdeRecalc = 5,
  odeRecalcFactor = 10^(0.5),
  indTolRelax = TRUE,
  useColor = NULL,
  printNcol = NULL,
  print = 1L,
  optExpression = TRUE,
  literalFix = TRUE,
  sumProd = FALSE,
  rxControl = NULL,
  calcTables = TRUE,
  compress = TRUE,
  adjObf = TRUE,
  ci = 0.95,
  sigdig = 4,
  sigdigTable = NULL,
  addProp = c("combined2", "combined1"),
  ...
)

nlmerControl(
  optimizer = "bobyqa",
  tolPwrss = NULL,
  optCtrl = list(),
  returnNlmer = FALSE,
  muRefCovAlg = TRUE,
  eventSens = c("jump", "fd"),
  eventType = c("central", "forward"),
  shiErr = (.Machine$double.eps)^(1/3),
  shi21maxFD = 20L,
  stickyRecalcN = 4,
  maxOdeRecalc = 5,
  odeRecalcFactor = 10^(0.5),
  indTolRelax = TRUE,
  useColor = NULL,
  printNcol = NULL,
  print = 1L,
  optExpression = TRUE,
  literalFix = TRUE,
  sumProd = FALSE,
  rxControl = NULL,
  calcTables = TRUE,
  compress = TRUE,
  adjObf = TRUE,
  ci = 0.95,
  sigdig = 4,
  sigdigTable = NULL,
  addProp = c("combined2", "combined1"),
  ...
)
}
\arguments{
\item{optimizer}{passed to \code{\link[lme4:nlmerControl]{lme4::nlmerControl()}}: optimizer to use}

\item{tolPwrss}{passed to \code{\link[lme4:nlmerControl]{lme4::nlmerControl()}}: tolerance for the
penalized, weighted residual sum-of-squares (PWRSS) inner iterations.
When \code{NULL} (the default) it is derived from \code{sigdig}, keeping the
\code{lme4} default of \code{1e-7} at the default \code{sigdig = 4} and
tightening/loosening it one order of magnitude per significant digit}

\item{optCtrl}{a \code{list} of additional control parameters passed to the
nonlinear optimizer via \code{\link[lme4:nlmerControl]{lme4::nlmerControl()}}}

\item{returnNlmer}{logical; when \code{TRUE} return the raw lme4 nlmerMod
object instead of the nlmixr2 fit}

\item{muRefCovAlg}{logical; when \code{TRUE} apply mu2 covariate
referencing algebraic transformation (same as saem)}

\item{eventSens}{method used for the dosing-parameter (alag/F/rate/dur)
sensitivities in the analytic gradient model: \code{"jump"} routes them
through rxode2's analytic event jumps; \code{"fd"} falls back to Shi2021
finite differences.  See \code{\link[nlmixr2est:nlmControl]{nlmixr2est::nlmControl()}}.}

\item{eventType}{finite-difference type (\code{"central"} or \code{"forward"})
used for event-related parameters when \code{eventSens = "fd"}}

\item{shiErr}{epsilon used when optimizing the ideal finite-difference
step size with the Shi2021 method}

\item{shi21maxFD}{maximum number of Shi2021 step-size optimization
iterations for the gradient}

\item{stickyRecalcN}{number of bad ODE solves tolerated before the
per-subject tolerance is stickily relaxed}

\item{maxOdeRecalc}{maximum number of times to retry a bad ODE solve
with a relaxed tolerance}

\item{odeRecalcFactor}{factor by which atol/rtol are relaxed on an ODE
solve retry}

\item{indTolRelax}{when \code{TRUE} (default) a subject whose ODE solve had to
be retried with a relaxed tolerance keeps that relaxed tolerance for the
rest of the fit instead of resetting it every evaluation}

\item{useColor}{Logical (or `NULL`) emit ANSI bold/color escapes in the
iteration print. `NULL` (default) defers to [crayon::has_color()].}

\item{printNcol}{Integer (or `NULL`) parameter columns per row before
wrapping. `NULL` (default) uses `floor((getOption("width") - 23) / 12)`.}

\item{print}{Either a scalar print-frequency (`0` = suppress, `1`
(default) = every evaluation, `N` = every Nth), OR a pre-built
[iterPrintControl()] object. Equivalent to
`iterPrintControl(every = print, ncol = printNcol, useColor = useColor)`.}

\item{optExpression}{Optimize the rxode2 expression to speed up
calculation. By default this is turned on.}

\item{literalFix}{boolean, substitute fixed population values as
literals and re-adjust ui and parameter estimates after
optimization; Default is `TRUE`.}

\item{sumProd}{Is a boolean indicating if the model should change
multiplication to high precision multiplication and sums to
high precision sums using the PreciseSums package.  By default
this is \code{FALSE}.}

\item{rxControl}{`rxode2` ODE solving options during fitting, created with `rxControl()`}

\item{calcTables}{This boolean is to determine if the foceiFit
will calculate tables. By default this is \code{TRUE}}

\item{compress}{Should the object have compressed items}

\item{adjObf}{is a boolean to indicate if the objective function
should be adjusted to be closer to NONMEM's default objective
function.  By default this is \code{TRUE}}

\item{ci}{Confidence level for some tables.  By default this is
0.95 or 95\% confidence.}

\item{sigdig}{Optimization significant digits.  One value drives, with a single
consistent formula, the inner/outer optimizer convergence tolerance
(\code{10^-sigdig}), the boundary check tolerance (\code{5*10^(-sigdig+1)}),
and the ODE solver tolerances: the \code{rtol} exponent IS \code{sigdig} and
\code{atol} sits three orders below, so \code{rtol = 10^-sigdig},
\code{atol = 10^(-sigdig-3)} for every solver (stiff, non-stiff or
auto-switching).  The sensitivity (\code{atolSens}/\code{rtolSens})
tolerances match the main solve (the outer gradient and covariance are built
from them); the steady-state (\code{ssAtol}/\code{ssRtol}) tolerances run one
order looser.
Keying the optimizer to the same \code{10^-sigdig} means it converges to
exactly the precision the solve supports.  At the default \code{sigdig = 3}
this is \code{atol = 1e-6}, \code{rtol = 1e-3}.}

\item{sigdigTable}{Significant digits in the final output table.
If not specified, then it matches the significant digits in the
`sigdig` optimization algorithm.  If `sigdig` is NULL, use 3.}

\item{addProp}{Type of additive-plus-proportional error: `"combined1"`,
where standard deviations add:
\deqn{y = f + (a + b\times f^c) \times \varepsilon}{y = f + (a + b*f^c)*err};
or `"combined2"`, where variances add:
\deqn{y = f + \sqrt{a^2 + b^2\times f^{2\times c}} \times \varepsilon}{y = f + sqrt(a^2 + b^2*(f^c)^2)*err}.
Here y = observed, f = predicted, a = additive sd, b = proportional/power
sd, c = power exponent (1 in the proportional case).}

\item{...}{Ignored parameters}
}
\value{
nlmer control structure
}
\description{
Control for nlmer estimation method in nlmixr2
}
\examples{
nlmerControl()
nlmixr2NlmerControl()
}
\author{
Matthew L. Fidler
}
