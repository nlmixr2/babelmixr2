% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pharmmlXml.R
\name{pharmmlValidate}
\alias{pharmmlValidate}
\title{Validate a PharmML document against its schema}
\usage{
pharmmlValidate(x, version = "0.9", error = TRUE)
}
\arguments{
\item{x}{Either a path to a PharmML file, or a length-one character vector
holding the document itself, or an \code{xml_document}.}

\item{version}{PharmML version to validate against.  See
\code{pharmmlVersions()}.}

\item{error}{When \code{TRUE} (default) an invalid document raises an error
listing the schema violations.  When \code{FALSE} the function returns \code{FALSE}
with the violations in the \code{"errors"} attribute.}
}
\value{
\code{TRUE} when the document validates.  When \code{error = FALSE} and the
document does not validate, \code{FALSE} with an \code{"errors"} attribute.
}
\description{
Validation is entirely offline: the schemas are vendored in the package and
their \code{schemaLocation} references have been rewritten to relative paths.
The upstream schema host (\code{pharmml.org}) no longer serves them.
}
\examples{
\dontrun{
pharmmlValidate("model.xml")
}
}
\author{
Matthew L. Fidler
}
